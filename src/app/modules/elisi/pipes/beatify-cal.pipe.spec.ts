import { BeatifyCalPipe } from './beatify-cal.pipe';

describe('BeatifyCalPipe', () => {
  it('create an instance', () => {
    const pipe = new BeatifyCalPipe();
    expect(pipe).toBeTruthy();
  });
});
